# AI代码安全审计CLI工具 - 环境变量配置示例
# 请将本文件复制为 .env 并填入你的实际配置值

# =============================================================================
# 必要配置 (必须设置)
# =============================================================================

# OpenAI API密钥 - 用于AI驱动的代码安全分析
# 获取方式：访问 https://platform.openai.com/api-keys 创建新的API密钥
OPENAI_API_KEY=your-openai-api-key-here

# =============================================================================
# 可选配置 (根据需要设置)
# =============================================================================

# OpenAI API基础URL (可选，一般不需要修改)
# 如果你使用OpenAI的代理服务或企业版，可能需要修改此配置
OPENAI_API_BASE=https://api.openai.com/v1

# AI模型选择 (可选，默认使用gpt-4o-mini)
# 可选模型：gpt-4o-mini, gpt-3.5-turbo, gpt-4
# 建议使用gpt-4o-mini，它在成本和性能之间有很好的平衡
OPENAI_MODEL=gpt-4o-mini

# 请求超时时间 (可选，单位：秒，默认：60)
# 分析大文件时可能需要增加超时时间
REQUEST_TIMEOUT=60

# 最大重试次数 (可选，默认：3)
# API请求失败时的重试次数
MAX_RETRIES=3

# =============================================================================
# 高级配置 (一般不需要修改)
# =============================================================================

# 日志级别 (可选，可选值：DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# 是否启用详细日志 (可选，true/false)
VERBOSE_LOGGING=false

# 临时文件目录 (可选，默认使用系统临时目录)
TEMP_DIR=/tmp

# =============================================================================
# 配置说明
# =============================================================================
#
# 1. 获取OpenAI API密钥：
#    - 访问 https://platform.openai.com
#    - 注册/登录账号
#    - 进入 API Keys 页面
#    - 点击 "Create new secret key"
#    - 复制生成的密钥并填入上面的 OPENAI_API_KEY
#
# 2. 配置优先级：
#    - 环境变量 > .env文件 > 默认值
#    - 命令行参数可以覆盖所有配置
#
# 3. 安全提醒：
#    - 不要将你的API密钥提交到代码仓库
#    - 定期轮换API密钥
#    - 监控API使用量，避免意外费用
#
# =============================================================================