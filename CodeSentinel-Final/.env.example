# CodeSentinel 环境变量配置模板
# 请将此文件复制为 .env 并填入你的实际配置

# =============================================================================
# AI功能配置 (可选)
# =============================================================================

# OpenAI API密钥 - 用于AI驱动的代码安全分析
# 获取方式：访问 https://platform.openai.com/api-keys 创建新的API密钥
OPENAI_API_KEY=your-openai-api-key-here

# OpenAI API基础URL (可选，一般不需要修改)
OPENAI_API_BASE=https://api.openai.com/v1

# AI模型选择 (可选，默认使用gpt-4o-mini)
# 可选模型：gpt-4o-mini, gpt-3.5-turbo, gpt-4
OPENAI_MODEL=gpt-4o-mini

# 请求超时时间 (可选，单位：秒，默认：60)
REQUEST_TIMEOUT=60

# 最大重试次数 (可选，默认：3)
MAX_RETRIES=3

# =============================================================================
# 基本配置 (可选)
# =============================================================================

# 日志级别 (可选，可选值：DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# 是否启用详细日志 (可选，true/false)
VERBOSE_LOGGING=false

# 临时文件目录 (可选，默认使用系统临时目录)
TEMP_DIR=/tmp

# =============================================================================
# 分析器配置 (可选)
# =============================================================================

# 默认分析器类型 (可选：local, ai, hybrid, multi_language)
ANALYZER_TYPE=multi_language

# 严重程度阈值 (可选：low, medium, high, critical, all)
SEVERITY_THRESHOLD=all

# 最大文件大小限制 (可选，单位：KB)
MAX_FILE_SIZE=1024

# 并发限制数量 (可选，默认：5)
CONCURRENT_LIMIT=5

# =============================================================================
# 安全配置 (可选)
# =============================================================================

# 缓存功能开关 (可选，默认：true)
CACHE_ENABLED=true

# 缓存过期时间 (可选，单位：秒，默认：3600)
CACHE_TTL=3600

# 隐私保护级别 (可选：none, basic, full)
PRIVACY_MODE=basic

# =============================================================================