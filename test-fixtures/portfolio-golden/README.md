# Portfolio Batch Golden Fixtures

Deterministic assertions for manifest-driven `batch-scan` orchestration.

Suggested generation command:

```powershell
.\codesentinel.ps1 batch-scan test-fixtures\portfolio-batch\manifest.json -Format json -Output reports\portfolio-batch.json

.\codesentinel.ps1 batch-scan test-fixtures\portfolio-batch\manifest-invalid.json -Format json -Output reports\portfolio-batch-invalid.json
```
